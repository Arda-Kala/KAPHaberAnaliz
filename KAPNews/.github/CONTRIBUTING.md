# Katkıda Bulunma

Pull request göndermeden önce:
- Gizli değer (API key, şifre, connection string) içermediğinden emin ol
- `appsettings.json`'u değiştirmeden User Secrets kullan
- `bin/`, `obj/`, `logs/` klasörlerini commit etme